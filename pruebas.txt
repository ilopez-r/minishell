cat Makefile >> b | cat <b | grep a | wc -l >> c

ls >> a >> b >> c >> d

<infile cat >> a >> b >> c >> d

cat infile wc -l

<< EOf cat >b | wc -l <b >> c

cat | cat | ls

para señales comando que bloquee:
bash-3.2$ cat
bash-3.2$ cat
^C
bash-3.2$ cat
^\Quit: 3